# Інструкція з тестування бота

Цей документ описує, як протестувати бота перед реальним запуском.

## 🧪 Крок 1: Перевірка компонентів

Спочатку перевірте, чи всі компоненти працюють правильно:

```bash
source venv/bin/activate
python test_components.py
```

Цей скрипт перевірить:
- ✅ Базу даних (SQLite)
- ✅ Google Search API
- ✅ Pexels API
- ✅ WordPress REST API підключення
- ✅ OpenAI API
- ✅ Повний workflow (опціонально, створює draft статтю)

**Очікуваний результат**: Всі тести повинні пройти успішно.

---

## 🧪 Крок 2: Тестовий режим (DRAFT)

Запустіть бота в тестовому режимі, який:
- Зберігає статті як **DRAFT** (чернетки) замість публікації
- Має короткі інтервали (1-2 хвилини замість 80-100)
- Обмежується 3 статтями для тесту

```bash
source venv/bin/activate
python run_test_mode.py
```

**Що відбувається**:
1. Бот створить 3 тестові статті як чернетки
2. Ви зможете переглянути їх в WordPress адмін-панелі
3. Перевірити якість контенту, зображень, форматування

**Перегляд чернеток**:
- Зайдіть в WordPress адмін-панель
- Перейдіть: **Пости → Всі пости**
- Знайдіть пости з префіксом `[TEST]`
- Перевірте контент та зображення

---

## 🧪 Крок 3: Тест зі змінною середовища

Альтернативний спосіб - використати змінну середовища для тестового режиму:

```bash
# Додайте в .env файл:
WP_POST_STATUS=draft

# Потім запустіть звичайний бот:
python run.py
```

Бот буде зберігати всі статті як чернетки, але з нормальними інтервалами (80-100 хвилин).

**Для повернення до публікації**: Видаліть або закоментуйте `WP_POST_STATUS` в `.env`.

---

## ✅ Перевірка перед реальним запуском

Перед запуском бота в продакшн режимі переконайтеся:

### 1. Всі API ключі валідні
```bash
python test_config.py
```

### 2. WordPress REST API працює
- Перевірте доступ до `/wp-json/wp/v2/posts`
- Переконайтеся, що Application Password правильний

### 3. Тестові статті виглядають добре
- Перевірте формат HTML
- Перевірте зображення
- Перевірте SEO-оптимізацію

### 4. База даних працює
- Файл `history.db` створюється автоматично
- Перевірте, що дублікати не створюються

---

## 🚀 Реальний запуск

Коли все протестовано і готово:

1. **Видаліть тестові чернетки** з WordPress (якщо потрібно)

2. **Переконайтеся, що `.env` не містить `WP_POST_STATUS=draft`**

3. **Запустіть бота**:
   ```bash
   source venv/bin/activate
   python run.py
   ```

4. **Моніторинг**:
   - Спостерігайте за логами
   - Перевіряйте опубліковані статті на сайті
   - Перевіряйте базу даних `history.db`

---

## 🐛 Вирішення проблем під час тестування

### Помилка: "Google search failed"
- **Причина**: Google може блокувати запити
- **Рішення**: Це не критично, бот продовжить роботу з порожнім контекстом

### Помилка: "Pexels API error"
- **Причина**: Неправильний API ключ або перевищення ліміту
- **Рішення**: Перевірте ключ в `.env`, бот продовжить без зображення

### Помилка: "WordPress publish failed"
- **Причина**: Неправильний Application Password або права доступу
- **Рішення**: 
  - Перевірте `WP_APP_PASSWORD` в `.env`
  - Переконайтеся, що це Application Password, а не звичайний пароль
  - Перевірте права користувача в WordPress

### Помилка: "OpenAI API error"
- **Причина**: Неправильний ключ або перевищення ліміту
- **Рішення**: Перевірте ключ та баланс на platform.openai.com

---

## 📊 Моніторинг тестів

### Перевірка бази даних

```bash
# Подивитися, які комбінації вже опубліковані
sqlite3 history.db "SELECT * FROM posted;"
```

### Перевірка логів

Всі дії бота логуються в консоль. Зверніть увагу на:
- `[SUCCESS]` - успішні операції
- `[ERROR]` - помилки, які потребують уваги

---

## 💡 Поради

1. **Почніть з тестового режиму** - не запускайте одразу в продакшн
2. **Перевірте перші статті вручну** - переконайтеся, що контент якісний
3. **Моніторте перші години** - перевіряйте, чи все працює правильно
4. **Зберігайте логи** - можна перенаправити вивід в файл:
   ```bash
   python run.py > bot.log 2>&1
   ```

---

**Готово до тестування! 🧪**


