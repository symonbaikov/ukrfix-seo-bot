# UkrFix SEO Bot

Автономний сервіс для **Programmatic SEO**, який автоматично генерує та публікує SEO-статті на WordPress сайті UkrFix.com.

---

## 🎯 Опис проекту

**UkrFix SEO Bot** — це бот для автоматичної генерації SEO-контенту за матрицею `[Країна] → [Місто] → [Категорія]`.

### Основні можливості:

- ✅ **Автоматична генерація статей** через OpenAI API
- ✅ **Пошук релевантних зображень** через Pexels API
- ✅ **Збір контексту** з Google для більш релевантного контенту
- ✅ **Публікація в WordPress** через REST API
- ✅ **Захист від дублікатів** через SQLite базу даних
- ✅ **Автоматичний цикл роботи** (10-15 статей на добу)
- ✅ **Готовий до роботи 24/7** на VPS або сервері

### Як це працює:

Бот вибирає випадкову комбінацію (наприклад: "Чехія → Прага → Ремонт квартир"), перевіряє, чи вже була створена стаття для цієї комбінації, збирає актуальну інформацію з Google, знаходить зображення, генерує статтю через AI та публікує її на сайті.

---

## 🏗️ Архітектура

Проект побудований за модульним принципом з чітким розподілом відповідальності:

### Шари архітектури:

1. **Application Layer** (`src/bot.py`)

   - Оркестрація роботи бота
   - Головний цикл та управління паузами

2. **Domain/Business Logic**

   - Генерація контенту (`src/seo_generator.py`)
   - Збір контексту (`src/google_context.py`)
   - Робота з базою даних (`src/database.py`)
   - Моделі даних (`src/models/`)

3. **Integration Layer**

   - OpenAI API (`src/seo_generator.py`)
   - Pexels API (`src/image_service.py`)
   - WordPress REST API (`src/wp_publisher.py`)

4. **Infrastructure Layer**
   - Конфігурація (`src/config.py`)
   - Логування (`src/utils/logger.py`)
   - Утиліти (`src/utils/helpers.py`)

### Принципи:

- **Single Responsibility** — кожен модуль відповідає за одну задачу
- **No hard-coded secrets** — всі ключі через `.env`
- **Розширюваність** — додавання нових країн/міст/категорій через `models/`
- **Прозорість** — все логується

---

## 🚀 Встановлення

### Вимоги

- Python 3.10 або вище
- Доступ до інтернету
- API ключі (див. розділ "Налаштування")

### Крок 1: Клонування репозиторію

```bash
git clone <repository-url>
cd ukrfix-seo-bot
```

### Крок 2: Створення віртуального середовища

```bash
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

### Крок 3: Встановлення залежностей

```bash
pip install -r requirements.txt
```

Встановлюються такі пакети:

- `openai` — для генерації статей
- `requests` — для HTTP запитів
- `googlesearch-python` — для пошуку в Google
- `python-dotenv` — для завантаження змінних середовища

---

## ⚙️ Налаштування

### Крок 1: Створення файлу `.env`

Скопіюйте приклад конфігурації:

```bash
cp env.example .env
```

### Крок 2: Заповнення API ключів

Відредагуйте файл `.env` та додайте реальні значення:

```env
# WordPress Configuration
WP_URL=https://ukrfix.com
WP_USERNAME=ваш_логін_wordpress
WP_APP_PASSWORD=ваш_пароль_додатку_wordpress

# OpenAI Configuration
OPENAI_API_KEY=sk-proj-...

# Pexels Configuration
PEXELS_API_KEY=ваш_ключ_pexels
```

### Отримання API ключів

#### WordPress Application Password

1. Увійдіть в адмін-панель WordPress
2. Перейдіть: **Профіль → Application Passwords**
3. Створіть новий пароль додатку
4. Скопіюйте його в `WP_APP_PASSWORD`

#### OpenAI API Key

1. Зареєструйтеся на [platform.openai.com](https://platform.openai.com)
2. Створіть API ключ в розділі [API Keys](https://platform.openai.com/api-keys)
3. Додайте ключ в `OPENAI_API_KEY`

#### Pexels API Key

1. Зареєструйтеся на [pexels.com/api](https://www.pexels.com/api/)
2. Отримайте безкоштовний API ключ
3. Додайте ключ в `PEXELS_API_KEY`

### Крок 3: Перевірка конфігурації

Запустіть тестовий скрипт:

```bash
python test_config.py
```

Якщо все налаштовано правильно, ви побачите:

```
✓ All configuration variables are set correctly!
Bot is ready to run!
```

---

## 🎮 Використання

### Запуск бота

```bash
source venv/bin/activate  # Якщо ще не активовано
python run.py
```

### Що відбувається при запуску:

1. **Ініціалізація** — створюється база даних `history.db`
2. **Вибір завдання** — випадкова комбінація (країна → місто → категорія)
3. **Перевірка дублікатів** — чи вже була створена стаття для цієї комбінації
4. **Збір контексту** — пошук в Google для актуальної інформації
5. **Пошук зображення** — знаходження релевантного фото в Pexels
6. **Завантаження зображення** — завантаження в WordPress медіа-бібліотеку
7. **Генерація статті** — створення контенту через OpenAI
8. **Публікація** — публікація статті на сайті
9. **Запис в базу** — збереження інформації про опубліковану статтю
10. **Пауза** — очікування 80-100 хвилин до наступної статті

### Зупинка бота

Натисніть `Ctrl+C` для коректної зупинки.

### Логи

Всі дії бота логуються в консоль з префіксом `[UKRFIX-SEO-BOT]`:

```
[UKRFIX-SEO-BOT] Bot started for UkrFix!
[UKRFIX-SEO-BOT] Working on: Ремонт квартир in Прага, Чехія
[UKRFIX-SEO-BOT] Image found, uploading...
[UKRFIX-SEO-BOT] Generating article...
[UKRFIX-SEO-BOT] Publishing...
[UKRFIX-SEO-BOT] [SUCCESS] Done! Article '...' published.
```

---

## 📁 Структура проекту

```
ukrfix-seo-bot/
├── src/                          # Основний код
│   ├── bot.py                   # Головний цикл бота
│   ├── config.py                # Конфігурація (доступ до .env)
│   ├── database.py              # Робота з SQLite
│   ├── google_context.py        # Пошук контексту в Google
│   ├── image_service.py         # Pexels API + завантаження в WP
│   ├── seo_generator.py         # Генерація статей через OpenAI
│   ├── wp_publisher.py          # Публікація в WordPress
│   ├── models/                  # Моделі даних
│   │   ├── location_map.py      # Матриця країн/міст
│   │   └── category_map.py      # Матриця категорій
│   └── utils/                   # Утиліти
│       ├── logger.py            # Централізоване логування
│       └── helpers.py           # Допоміжні функції
├── docs/                        # Документація
│   ├── plan.md                 # План проекту
│   └── integrations.md         # Опис інтеграцій
├── run.py                      # Точка входу
├── test_config.py              # Тест конфігурації
├── requirements.txt            # Залежності Python
├── env.example                 # Приклад .env файлу
├── .gitignore                  # Git ignore правила
└── README.md                   # Цей файл
```

### Опис модулів

#### `src/bot.py`

Головний оркестратор. Відповідає за:

- Вибір завдання (комбінація країна-місто-категорія)
- Послідовний виклик всіх модулів
- Управління паузами та циклом
- Обробку помилок верхнього рівня

#### `src/config.py`

Єдина точка доступу до змінних середовища. Всі модулі отримують конфігурацію через цей файл.

#### `src/database.py`

Робота з SQLite базою даних:

- `init_db()` — ініціалізація таблиці `posted`
- `is_posted()` — перевірка, чи вже опублікована комбінація
- `mark_posted()` — запис про публікацію

#### `src/google_context.py`

Збір контексту з Google для більш релевантного контенту. Повертає текст з описів топ-результатів пошуку.

#### `src/image_service.py`

- `get_pexels_image()` — пошук зображення в Pexels
- `upload_image_to_wp()` — завантаження зображення в WordPress

#### `src/seo_generator.py`

Генерація SEO-статей через OpenAI API. Формує промпт та повертає `(title, html_content)`.

#### `src/wp_publisher.py`

Публікація статей в WordPress через REST API. Обробляє помилки без падіння бота.

---

## 🔌 API та інтеграції

### OpenAI API

- **Модель**: `gpt-4o`
- **Використання**: Генерація HTML-контенту статей
- **Параметри**: Тема статті, контекст з Google, структура SEO-статті

### Pexels API

- **Використання**: Пошук релевантних зображень за ключовими словами
- **Формат**: Large landscape зображення
- **Ліцензія**: Безкоштовні легальні фото

### WordPress REST API

- **Авторизація**: Application Password
- **Ендпоінти**:
  - `/wp-json/wp/v2/media` — завантаження зображень
  - `/wp-json/wp/v2/posts` — публікація статей
- **Параметри публікації**:
  - `title` — заголовок статті
  - `content` — HTML контент
  - `featured_media` — ID зображення
  - `status` — `publish` (публікація одразу)

### Google Search

- **Бібліотека**: `googlesearch-python`
- **Використання**: Парсинг видачи Google для збору контексту
- **Результати**: 2-3 описи конкуруючих сайтів

---

## 🔄 Логіка роботи

### Потік даних (Data Flow)

1. **Вибір завдання**

   - `bot.py` → вибір випадкової комбінації з `LOCATIONS` та `CATEGORIES`
   - Перевірка через `database.is_posted()` — якщо вже є, шукає іншу

2. **Контекст**

   - Формування запиту: `"оголошення {category} {city} {country} форуми"`
   - `google_context.get_google_context(query)` → текст контексту

3. **Зображення**

   - Береться англійський запит з `CATEGORIES[category]`
   - `image_service.get_pexels_image(query)` → URL
   - `image_service.upload_image_to_wp(url, title)` → `media_id`

4. **Текст**

   - `seo_generator.generate_article(...)` → `(title, html_content)`

5. **Публікація**

   - `wp_publisher.publish_article(title, html, media_id)`

6. **Історія**

   - `database.mark_posted(country, city, category)`

7. **Пауза**
   - `time.sleep(random.randint(4800, 6000))` (80-100 хвилин)

### Обробка помилок

- **Мережеві помилки** (OpenAI, Pexels, WP, Google):

  - Логуються
  - Не зупиняють бота
  - Бот робить спробу з іншою комбінацією або йде в sleep

- **Помилки БД**:

  - Критичні → лог та завершення процесу
  - Краще впасти, ніж пошкодити історію

- **Idempotent логіка**:
  - Якщо один крок не пройшов, наступний цикл бере нову задачу

### Розклад роботи

- **Ціль**: 10-15 статей на добу
- **Інтервал**: 80-100 хвилин між статтями
- **Розрахунок**: 24 години = 1440 хвилин / 15 статей = ~96 хвилин

---

## 📊 Матриця контенту

### Країни та міста (`src/models/location_map.py`)

```python
LOCATIONS = {
    "Чехія": ["Прага", "Брно", "Острава", ...],
    "Польща": ["Варшава", "Краков", ...],
    "Україна": ["Київ", "Львів", ...],
    "Німеччина": ["Берлін", "Мюнхен", ...]
}
```

### Категорії (`src/models/category_map.py`)

```python
CATEGORIES = {
    "Ремонт квартир": "home renovation worker",
    "Краса та здоров'я": "beauty salon spa",
    "Побутові послуги": "plumber electrician",
    # ...
}
```

**Додавання нових комбінацій**: Просто додайте їх в відповідні файли в `src/models/`.

---

## 🔧 Розширення

### Потенційні покращення:

1. **Панель моніторингу**

   - Web-сервіс (FastAPI/Flask) поверх `history.db`
   - Перегляд опублікованих статей, логів, статусу бота

2. **Телеграм-сповіщення**

   - Модуль `notifications/telegram_notifier.py`
   - Відправка статусу після кожної публікації

3. **Шаблони контенту**

   - Різні prompt-профілі для різних категорій
   - Зберігання шаблонів в `templates/`

4. **Планувальник**
   - Використання APScheduler або cron замість ручного sleep

---

## 🐛 Вирішення проблем

### Помилка: "environment variable is not set"

**Рішення**: Перевірте, чи створений файл `.env` та чи всі змінні заповнені.

### Помилка: "No matching distribution found"

**Рішення**: Переконайтеся, що використовуєте правильну назву пакета в `requirements.txt` (`googlesearch-python`, не `google-search-python`).

### Бот не публікує статті

**Перевірте**:

1. Чи правильний `WP_APP_PASSWORD` (Application Password, не звичайний пароль)
2. Чи доступний WordPress REST API
3. Чи є права на публікацію у користувача

### Google Search не працює

**Примітка**: Бібліотека `googlesearch-python` може іноді давати помилки (Too many requests). Для стабільної роботи на великих обсягах краще використовувати API (наприклад, SerpApi), але для 10-15 статей на добу може вистачити і цього.

---

## 📝 Ліцензія

Цей проект розроблений для UkrFix.com.

---
